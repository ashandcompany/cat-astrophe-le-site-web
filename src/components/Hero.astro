---
---
<section class="hero">
  <div class="container">
    <div class="hero-content">
      <h1>Semez le chaos !</h1>
      <p>Vous êtes un chat dans une maison remplie d'objets fragiles.</br>Dur de résister non ?</p>
      <a href="#gallery" class="cta-button">Détruisez Tout !</a>
    </div>
  </div>
</section>

<style>
  .hero {
    background-color: var(--color-primary);
    min-height: 80vh;
    display: flex;
    align-items: center;
    position: relative;
    overflow: hidden;
  }

  .hero::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: url('/images/image.png') center/cover;
    opacity: 0.4;
    z-index: 1;
  }

  .hero-content {
    position: relative;
    z-index: 2;
    text-align: center;
    color: white;
  }

  h1 {
    font-size: 4rem;
    margin-bottom: 1rem;
    color: var(--color-secondary);
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
  }

  p {
    font-size: 1.5rem;
    margin-bottom: 2rem;
    color: #333;
  }

  .cta-button {
    display: inline-block;
    padding: 1rem 2rem;
    background-color: var(--color-secondary);
    color: white;
    text-decoration: none;
    border-radius: 50px;
    font-size: 1.2rem;
    transition: transform 0.3s ease;
  }

  .cta-button:hover {
    transform: scale(1.05);
  }
</style>